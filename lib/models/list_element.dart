import 'package:flutter/material.dart';

class PlacesList {
  final String label;
  final IconData icon;
  final Color color;

  const PlacesList({
    required this.label,
    required this.icon,
    required this.color,
  });
}
